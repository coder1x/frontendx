extends ../layout/layout.pug
include ../../components/header/header.pug
include ../../components/side-panel/side-panel.pug
include ../../components/bread-crumbs/bread-crumbs.pug

block title 
  title Записи
block header
  +header({hasPanelButton: true})
block content
  - const BASE_CLASS = 'layout-with-panel';
  div(class = `${BASE_CLASS} js-${BASE_CLASS}`)
    section(class = `${BASE_CLASS}__bread-crumbs-wrapper`)
      +bread-crumbs()
    block main
    section(class = `${BASE_CLASS}__side-panel-wrapper js-${BASE_CLASS}__side-panel-wrapper`)
      +side-panel()
    block pagination